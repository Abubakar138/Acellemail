<?php

return [
    '1' => [
        'url' => 'http://localhost:3000',
        'db_name' => 'wordpress',
        'db_prefix' => '',

        // 'db_port' => '3306', // same as acelle config
        // 'db_host' => 'localhost', // same as acelle config
        // 'db_user' => '', // same as acelle config
        // 'db_password' => '', // same as acelle config
    ],
    '2' => [
        'url' => 'http://localhost:3001',
        'db_name' => 'wordpress',
        'db_prefix' => '',

        // 'db_port' => '3306', // same as acelle config
        // 'db_host' => 'localhost', // same as acelle config
        // 'db_user' => '', // same as acelle config
        // 'db_password' => '', // same as acelle config
    ],
];
